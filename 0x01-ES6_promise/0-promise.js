function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Make API call here
    // On success, call resolve() with the response
    // On failure, call reject() with the error
  });
}
